# 执行计划：Harness Bootstrap

## 目标

创建 `agent-trend-radar` 项目骨架和完整 SDD/Harness 文档体系。

## 非目标

- 本次不实现业务代码。
- 本次不接入 Trendshift 真实抓取。
- 本次不生成 daily report。

## 步骤

1. 创建项目基础目录。
2. 创建 `config.yaml` 和 TypeScript 项目占位。
3. 创建 `data/` 落盘目录。
4. 创建 `docs/specs/` Harness。
5. 写入系统级、服务级、产品级、约束、反馈和执行计划。

## 验证

- 文件结构枚举。
- 关键 Spec 引用搜索。
- 后续实现前再运行 TypeScript / Vitest。

