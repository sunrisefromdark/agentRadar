# Repo Policy

## Runtime Environment

Use the project-specific Node.js and pnpm environment before running validation or generation commands.

## Design Approval Rule

Do not create or update a matching `docs/specs/exec-plans/*.exec-plan.md` until the referenced design document is explicitly marked as approved.
